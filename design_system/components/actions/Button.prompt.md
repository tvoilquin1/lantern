Button. One primary per screen; always pair a heavy ask with a `quiet` way out.

```jsx
<Button>Talk this through with Lantern</Button>
<Button variant="quiet">Not now</Button>
```

Never label a button with an imperative that implies obligation ("Complete your check-in"). Lantern invites.

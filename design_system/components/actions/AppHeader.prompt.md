AppHeader tops every screen.

```jsx
<AppHeader eyebrow="Tuesday, 26 August" title="Good morning, Sarah." action={<StatePill tone="neutral">Today ›</StatePill>} />
```

The conversation is home, so the dashboard is reached from the header `action`, not from a tab bar.

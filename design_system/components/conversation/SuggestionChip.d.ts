import * as React from "react";

/** A tappable short answer, offered so the caregiver can reply on a bad morning without composing a sentence. */
export interface SuggestionChipProps extends React.ButtonHTMLAttributes<HTMLButtonElement> {}
export declare function SuggestionChip(props: SuggestionChipProps): React.JSX.Element;

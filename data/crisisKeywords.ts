// Crisis keyword list for the bypass layer that fires before LLM evaluation.
// Populated in Phase 1 (companion build). See AGENTS.md > Key resolved decisions > Crisis protocol.
export type CrisisKeyword = string;

export const crisisKeywords: CrisisKeyword[] = [];
